# 정적 사이트(빌드 불필요) — 루트의 index.html 을 그대로 서빙
[build]
  publish = "."
  command = ""

# 정적 자산 캐시로 인한 구버전 표시 방지: HTML/JS/CSS 는 항상 최신 확인
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
